{
  "name": "@avyn/initials-avatar-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@10",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rimraf packages/*/dist",
    "lint": "echo 'No lint step configured'",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "tsup": "^8.2.4",
    "typescript": "^5.6.3"
  }
}
